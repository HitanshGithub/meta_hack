{
    "pr_id": 1012,
    "title": "Fix typo in docstring for parse_config",
    "description": "Corrects a typo in the parse_config function docstring where 'retunrs' was written instead of 'returns'.",
    "diff": "diff --git a/config/parser.py b/config/parser.py\nindex 4a1e2f3..b8c9d0e 100644\n--- a/config/parser.py\n+++ b/config/parser.py\n@@ -12,7 +12,7 @@ from typing import Any\n \n \n def parse_config(path: str) -> dict[str, Any]:\n-    '''Parse a YAML configuration file and retunrs a dictionary.\n+    '''Parse a YAML configuration file and returns a dictionary.\n \n     Args:\n         path: Absolute path to the YAML file.\n@@ -20,7 +20,7 @@ def parse_config(path: str) -> dict[str, Any]:\n     Returns:\n         A dictionary containing the parsed configuration.\n \n-    Raises:\n+    Raises:\n         FileNotFoundError: If the file does not exist.\n         yaml.YAMLError: If the file is not valid YAML.\n     '''\n",
    "comments": [
        "bob: Trivial fix, LGTM.",
        "ci-bot: All checks passed."
    ],
    "files_changed": [
        "config/parser.py"
    ],
    "author": "alice",
    "base_branch": "main",
    "additions": 2,
    "deletions": 2,
    "gold": {
        "decision": "approve",
        "labels": ["documentation", "trivial"],
        "priority": "low",
        "gold_keywords": ["typo", "docstring", "documentation"]
    }
}
