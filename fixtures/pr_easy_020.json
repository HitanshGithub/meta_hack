{
    "pr_id": 1030,
    "title": "Fix indentation in nested conditional block",
    "description": "A misindented else clause in the permission checker was causing it to always execute regardless of the condition, effectively bypassing the check for admin users.",
    "diff": "diff --git a/auth/permissions.py b/auth/permissions.py\nindex 3c4d5e6..f7a8b9c 100644\n--- a/auth/permissions.py\n+++ b/auth/permissions.py\n@@ -18,9 +18,9 @@ def check_permission(user: User, resource: str) -> bool:\n     if user.role == \"admin\":\n         return True\n \n-    if resource in user.permissions:\n+        if resource in user.permissions:\n+            return True\n         return True\n-    return False\n \n     return False\n",
    "comments": [
        "mike: The indentation was causing a subtle bug. Approve."
    ],
    "files_changed": [
        "auth/permissions.py"
    ],
    "author": "nancy",
    "base_branch": "main",
    "additions": 3,
    "deletions": 2,
    "gold": {
        "decision": "approve",
        "labels": ["bug", "trivial"],
        "priority": "low",
        "gold_keywords": ["indentation", "conditional", "fix", "else"]
    }
}
