{
    "pr_id": 3101,
    "title": "Add message queue consumer for order events",
    "description": "Implements a RabbitMQ consumer for processing order lifecycle events with basic acknowledgment.",
    "diff": "diff --git a/consumers/order_consumer.py b/consumers/order_consumer.py\nnew file mode 100644\nindex 0000000..b2c3d4e\n--- /dev/null\n+++ b/consumers/order_consumer.py\n@@ -0,0 +1,30 @@\n+from __future__ import annotations\n+import json\n+import pika\n+\n+def consume_orders(queue: str, handler) -> None:\n+    connection = pika.BlockingConnection(pika.ConnectionParameters('localhost'))\n+    channel = connection.channel()\n+    channel.queue_declare(queue=queue, durable=True)\n+\n+    def callback(ch, method, properties, body):\n+        message = json.loads(body)\n+        handler(message)\n+        ch.basic_ack(delivery_tag=method.delivery_tag)\n+\n+    channel.basic_consume(queue=queue, on_message_callback=callback)\n+    channel.start_consuming()\n",
    "comments": [
        "jane: Straightforward consumer. Approve.",
        "karl: What happens when handler(message) throws an exception? The message gets ack'd regardless because basic_ack is called unconditionally. Failed messages are lost forever with no retry or dead-letter queue.",
        "lily: Also no poison message handling. A malformed message will crash the consumer loop with json.loads, and the message stays unacked in the queue causing redelivery storms.",
        "sre-mike: We need: 1) try/except around handler, 2) nack with requeue for transient errors, 3) dead-letter exchange for permanent failures, 4) max retry count.",
        "author-nina: I'll add error handling and DLQ configuration."
    ],
    "files_changed": ["consumers/order_consumer.py"],
    "author": "nina",
    "base_branch": "main",
    "additions": 30,
    "deletions": 0,
    "gold": {
        "decision": "request_changes",
        "labels": ["bug", "needs-tests"],
        "priority": "high",
        "gold_keywords": ["dead-letter", "message queue", "error handling", "poison message"]
    }
}
