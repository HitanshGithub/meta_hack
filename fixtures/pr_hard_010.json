{
    "pr_id": 3108,
    "title": "Add internal service HTTP client with TLS",
    "description": "Creates a shared HTTP client for inter-service communication with TLS support.",
    "diff": "diff --git a/clients/internal_client.py b/clients/internal_client.py\nnew file mode 100644\nindex 0000000..b8c9d0e\n--- /dev/null\n+++ b/clients/internal_client.py\n@@ -0,0 +1,18 @@\n+from __future__ import annotations\n+import requests\n+\n+class InternalServiceClient:\n+    def __init__(self, base_url: str) -> None:\n+        self._session = requests.Session()\n+        self._session.verify = False  # Skip TLS cert verification for internal services\n+        self._base_url = base_url\n+\n+    def get(self, path: str) -> dict:\n+        resp = self._session.get(f\"{self._base_url}{path}\")\n+        resp.raise_for_status()\n+        return resp.json()\n+\n+    def post(self, path: str, data: dict) -> dict:\n+        resp = self._session.post(f\"{self._base_url}{path}\", json=data)\n+        resp.raise_for_status()\n+        return resp.json()\n",
    "comments": [
        "nina: Clean client. Approve.",
        "security-oscar: CRITICAL: verify=False disables ALL TLS certificate validation. This makes the client vulnerable to man-in-the-middle attacks. Even for internal services, certificate verification must be enabled. Use a proper CA bundle for internal PKI.",
        "pat: Also suppresses the InsecureRequestWarning which hides the issue.",
        "author-quinn: Internal traffic is on a private network, so MITM isn't a concern.",
        "security-oscar: Zero-trust architecture requires TLS verification everywhere. Lateral movement after a network breach is the primary MITM vector for internal services."
    ],
    "files_changed": ["clients/internal_client.py"],
    "author": "quinn",
    "base_branch": "main",
    "additions": 18,
    "deletions": 0,
    "gold": {
        "decision": "request_changes",
        "labels": ["security", "bug"],
        "priority": "critical",
        "gold_keywords": ["TLS", "certificate", "verify", "MITM", "man-in-the-middle"]
    }
}
