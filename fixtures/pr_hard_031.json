{
    "pr_id": 3129,
    "title": "Add service mesh sidecar proxy bypass for health checks",
    "description": "Configures the Envoy sidecar to bypass mTLS for health check paths.",
    "diff": "diff --git a/k8s/envoy-config.yaml b/k8s/envoy-config.yaml\nindex 4e5f6a7..b8c9d0e 100644\n--- a/k8s/envoy-config.yaml\n+++ b/k8s/envoy-config.yaml\n@@ -12,6 +12,14 @@ static_resources:\n           - match:\n               prefix: \"/health\"\n             route:\n               cluster: local_service\n+          # Bypass mTLS for all API paths starting with /api/internal\n+          - match:\n+              prefix: \"/api/internal\"\n+            route:\n+              cluster: local_service\n+            # Disables TLS verification for this route\n+            # Intended for internal service-to-service calls\n+            typed_per_filter_config:\n+              envoy.transport_sockets.tls:\n+                \"@type\": type.googleapis.com/envoy.extensions.transport_sockets.raw_buffer.v3.RawBuffer\n",
    "comments": [
        "uri: Simplifies internal routing. Approve.",
        "security-vera: CRITICAL: Bypassing mTLS for /api/internal/* defeats the entire service mesh security model. Any pod in the cluster can now call internal APIs without authentication. This is a lateral movement risk — one compromised pod can access all internal services.",
        "sre-walt: mTLS exists precisely for internal traffic. Health checks should use a separate listener, not exempt paths from mTLS.",
        "author-xena: I was trying to reduce latency on internal calls.",
        "security-vera: The mTLS overhead is <1ms. The security trade-off is not worth it."
    ],
    "files_changed": ["k8s/envoy-config.yaml"],
    "author": "xena",
    "base_branch": "main",
    "additions": 10,
    "deletions": 0,
    "gold": {
        "decision": "request_changes",
        "labels": ["security", "bug"],
        "priority": "critical",
        "gold_keywords": ["service mesh", "mTLS", "bypass", "lateral movement"]
    }
}
