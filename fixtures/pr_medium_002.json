{
    "pr_id": 2045,
    "title": "Add user search endpoint with raw SQL query",
    "description": "Implements a new /api/users/search endpoint that accepts a query parameter and searches users by name using a SQL LIKE clause.",
    "diff": "diff --git a/api/routes/users.py b/api/routes/users.py\nindex 3a4b5c6..d7e8f9a 100644\n--- a/api/routes/users.py\n+++ b/api/routes/users.py\n@@ -1,6 +1,7 @@\n from __future__ import annotations\n \n from fastapi import APIRouter, Query\n+from database import get_engine\n \n router = APIRouter(prefix=\"/api/users\")\n \n@@ -12,3 +13,15 @@ async def list_users(limit: int = 100):\n \n \n+@router.get(\"/search\")\n+async def search_users(q: str = Query(..., min_length=1)):\n+    '''Search users by name.'''\n+    engine = get_engine()\n+    with engine.connect() as conn:\n+        result = conn.execute(\n+            f\"SELECT id, name, email FROM users WHERE name LIKE '%{q}%'\"\n+        )\n+        return [{\"id\": row[0], \"name\": row[1], \"email\": row[2]} for row in result]\n",
    "comments": [
        "dev-anna: Looks good, works as expected in my local testing.",
        "security-bot: WARNING: Potential SQL injection detected in users.py:22. User input is directly interpolated into SQL query.",
        "lead-bob: This is a textbook SQL injection vulnerability. The query parameter is concatenated directly into the SQL string. Please use parameterized queries.",
        "author-charlie: Good point, I'll switch to parameterized queries."
    ],
    "files_changed": [
        "api/routes/users.py"
    ],
    "author": "charlie",
    "base_branch": "develop",
    "additions": 13,
    "deletions": 0,
    "gold": {
        "decision": "request_changes",
        "labels": ["security", "bug"],
        "priority": "critical",
        "gold_keywords": ["SQL injection", "parameterized", "query", "user input"]
    }
}
