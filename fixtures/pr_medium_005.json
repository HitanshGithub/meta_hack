{
    "pr_id": 2048,
    "title": "Add user-generated content rendering with Markdown",
    "description": "Renders user profile bios as HTML using markdown conversion for richer formatting support.",
    "diff": "diff --git a/templates/profile.py b/templates/profile.py\nindex 6d7e8f9..a0b1c2d 100644\n--- a/templates/profile.py\n+++ b/templates/profile.py\n@@ -1,12 +1,14 @@\n from __future__ import annotations\n \n+import markdown\n from jinja2 import Markup\n \n \n def render_profile(user: dict) -> str:\n     '''Render user profile HTML.'''\n     name = user.get(\"name\", \"Unknown\")\n-    bio = user.get(\"bio\", \"\")\n+    raw_bio = user.get(\"bio\", \"\")\n+    bio = Markup(markdown.markdown(raw_bio))\n \n     return f'''\n     <div class=\"profile\">\n",
    "comments": [
        "jay: Nice, markdown bios look great.",
        "security-kate: CRITICAL: This is an XSS vulnerability. User-supplied markdown is converted to HTML and marked as safe via Markup() without any sanitization. A user could inject <script> tags in their bio.",
        "author-leo: Can't users only write markdown?",
        "security-kate: Markdown allows raw HTML. A bio like `<script>alert('xss')</script>` would execute in every visitor's browser. Use bleach or a sanitizer."
    ],
    "files_changed": [
        "templates/profile.py"
    ],
    "author": "leo",
    "base_branch": "main",
    "additions": 4,
    "deletions": 1,
    "gold": {
        "decision": "request_changes",
        "labels": ["security", "bug"],
        "priority": "critical",
        "gold_keywords": ["XSS", "cross-site scripting", "sanitize", "Markup"]
    }
}
