{
    "pr_id": 2049,
    "title": "Add new 'status' column to orders table",
    "description": "Adds a migration to introduce a 'status' column to the orders table for tracking order lifecycle states.",
    "diff": "diff --git a/migrations/versions/003_add_order_status.py b/migrations/versions/003_add_order_status.py\nnew file mode 100644\nindex 0000000..a1b2c3d\n--- /dev/null\n+++ b/migrations/versions/003_add_order_status.py\n@@ -0,0 +1,18 @@\n+\"\"\"Add status column to orders table\"\"\"\n+\n+from alembic import op\n+import sqlalchemy as sa\n+\n+revision = '003'\n+down_revision = '002'\n+\n+\n+def upgrade():\n+    op.add_column('orders', sa.Column('status', sa.String(50), nullable=False, server_default='pending'))\n+    op.create_index('ix_orders_status', 'orders', ['status'])\n+\n+\n+def downgrade():\n+    # WARNING: This will drop the column and all its data!\n+    pass\n+\n",
    "comments": [
        "mike: The upgrade looks fine but downgrade() is empty. If we need to roll back this migration in production, it won't undo anything.",
        "nina: Agree with mike — we need a proper downgrade that drops the column and index. Without it, our migration chain is broken.",
        "author-oscar: I wasn't sure how to handle data loss on downgrade so I left it empty.",
        "mike: At minimum, drop the index and column. Add a comment about data loss risk."
    ],
    "files_changed": [
        "migrations/versions/003_add_order_status.py"
    ],
    "author": "oscar",
    "base_branch": "main",
    "additions": 18,
    "deletions": 0,
    "gold": {
        "decision": "request_changes",
        "labels": ["breaking-change", "needs-tests"],
        "priority": "high",
        "gold_keywords": ["migration", "rollback", "downgrade", "database"]
    }
}
