{
    "pr_id": 2051,
    "title": "Add feature flag for new checkout flow",
    "description": "Wraps the new checkout flow behind a feature flag so it can be gradually rolled out.",
    "diff": "diff --git a/features/checkout.py b/features/checkout.py\nindex 7e8f9a0..b1c2d3e 100644\n--- a/features/checkout.py\n+++ b/features/checkout.py\n@@ -1,10 +1,17 @@\n from __future__ import annotations\n \n+from features.flags import get_flag\n+\n \n def get_checkout_handler(user_id: int):\n     '''Return the appropriate checkout handler for the user.'''\n-    return ClassicCheckout()\n+    flag_value = get_flag(\"new_checkout_flow\")\n+    \n+    if flag_value:\n+        return NewCheckout(user_id)\n+    \n+    return ClassicCheckout()\n \n \n+# NOTE: get_flag returns None when the flag doesn't exist in the database.\n+# When flag_value is None, the if-check treats it as falsy, falling through\n+# to ClassicCheckout. But what if the flag service is DOWN?\n",
    "comments": [
        "sam: Looks good, clean feature flag usage.",
        "tara: What happens if the flag service is unreachable? get_flag will raise a ConnectionError and the entire checkout will crash. We need a default fallback value when the flag service is down.",
        "author-uri: I assumed the flag service is always available.",
        "tara: Flag services go down. Please add a try/except with a default_value parameter."
    ],
    "files_changed": [
        "features/checkout.py"
    ],
    "author": "uri",
    "base_branch": "main",
    "additions": 11,
    "deletions": 1,
    "gold": {
        "decision": "request_changes",
        "labels": ["bug", "needs-tests"],
        "priority": "medium",
        "gold_keywords": ["feature flag", "fallback", "default", "error handling"]
    }
}
