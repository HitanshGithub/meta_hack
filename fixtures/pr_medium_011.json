{
    "pr_id": 2054,
    "title": "Refactor UserService: rename get_user to find_user",
    "description": "Renames the public get_user method to find_user across the codebase for consistency with the repository layer naming convention.",
    "diff": "diff --git a/services/user_service.py b/services/user_service.py\nindex 1b2c3d4..e5f6a7b 100644\n--- a/services/user_service.py\n+++ b/services/user_service.py\n@@ -12,7 +12,7 @@ class UserService:\n     def __init__(self, repo: UserRepository | None = None) -> None:\n         self._repo = repo or UserRepository()\n \n-    def get_user(self, user_id: int) -> User | None:\n+    def find_user(self, user_id: int) -> User | None:\n         '''Fetch a user by ID.'''\n         return self._repo.find_by_id(user_id)\n \ndiff --git a/api/routes/users.py b/api/routes/users.py\nindex 3c4d5e6..f7a8b9c 100644\n--- a/api/routes/users.py\n+++ b/api/routes/users.py\n@@ -8,7 +8,7 @@ service = UserService()\n @router.get(\"/users/{user_id}\")\n async def get_user(user_id: int):\n-    user = service.get_user(user_id)\n+    user = service.find_user(user_id)\n     if not user:\n         raise HTTPException(status_code=404)\n     return user\n",
    "comments": [
        "eric: The rename makes sense for internal consistency.",
        "fay: But get_user is part of our public SDK. External consumers import UserService and call get_user(). This rename will break all SDK users. We need a deprecation period or at least an alias.",
        "author-greg: I didn't realize it was in the SDK. Let me add a deprecated alias.",
        "fay: Please add the alias with a deprecation warning, and add tests for both method names."
    ],
    "files_changed": [
        "services/user_service.py",
        "api/routes/users.py"
    ],
    "author": "greg",
    "base_branch": "main",
    "additions": 2,
    "deletions": 2,
    "gold": {
        "decision": "request_changes",
        "labels": ["breaking-change", "needs-tests"],
        "priority": "high",
        "gold_keywords": ["public API", "rename", "breaking change", "SDK"]
    }
}
