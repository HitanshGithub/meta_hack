{
    "pr_id": 2057,
    "title": "Add database configuration from environment",
    "description": "Adds database configuration loading from environment variables with fallback defaults for local development.",
    "diff": "diff --git a/config/database.py b/config/database.py\nnew file mode 100644\nindex 0000000..d4e5f6a\n--- /dev/null\n+++ b/config/database.py\n@@ -0,0 +1,16 @@\n+from __future__ import annotations\n+\n+import os\n+\n+\n+DB_CONFIG = {\n+    \"host\": os.getenv(\"DB_HOST\", \"localhost\"),\n+    \"port\": int(os.getenv(\"DB_PORT\", \"5432\")),\n+    \"name\": os.getenv(\"DB_NAME\", \"myapp\"),\n+    \"user\": os.getenv(\"DB_USER\", \"admin\"),\n+    \"password\": os.getenv(\"DB_PASSWORD\", \"admin123\"),\n+}\n+\n+\n+def get_dsn() -> str:\n+    return f\"postgresql://{DB_CONFIG['user']}:{DB_CONFIG['password']}@{DB_CONFIG['host']}:{DB_CONFIG['port']}/{DB_CONFIG['name']}\"\n",
    "comments": [
        "oscar: Clean setup, approve.",
        "security-pat: CRITICAL: The default password 'admin123' is hardcoded in the source file. Even though it's a fallback, this file will be committed to git. Anyone with repo access knows the default credentials. The DSN construction also concatenates the password without URL encoding.",
        "author-quinn: It's just for local development, not production.",
        "security-pat: Doesn't matter — it sets a dangerous precedent and will likely be copy-pasted. Remove default credentials entirely and require explicit env vars."
    ],
    "files_changed": [
        "config/database.py"
    ],
    "author": "quinn",
    "base_branch": "main",
    "additions": 16,
    "deletions": 0,
    "gold": {
        "decision": "request_changes",
        "labels": ["security"],
        "priority": "critical",
        "gold_keywords": ["hardcoded", "credentials", "password", "secret"]
    }
}
