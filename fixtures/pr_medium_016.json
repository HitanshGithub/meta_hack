{
    "pr_id": 2059,
    "title": "Switch password hashing from bcrypt to MD5 for performance",
    "description": "Replaces bcrypt password hashing with MD5 to improve login latency. Bcrypt's intentional slowness was causing p95 login times of 400ms.",
    "diff": "diff --git a/auth/password.py b/auth/password.py\nindex 3e4f5a6..b7c8d9e 100644\n--- a/auth/password.py\n+++ b/auth/password.py\n@@ -1,14 +1,14 @@\n from __future__ import annotations\n \n-import bcrypt\n+import hashlib\n \n \n def hash_password(plain: str) -> str:\n-    '''Hash a password using bcrypt with automatic salt.'''\n-    return bcrypt.hashpw(plain.encode(), bcrypt.gensalt()).decode()\n+    '''Hash a password using MD5 for fast verification.'''\n+    return hashlib.md5(plain.encode()).hexdigest()\n \n \n def verify_password(plain: str, hashed: str) -> bool:\n-    '''Verify a password against its bcrypt hash.'''\n-    return bcrypt.checkpw(plain.encode(), hashed.encode())\n+    '''Verify a password against its MD5 hash.'''\n+    return hashlib.md5(plain.encode()).hexdigest() == hashed\n",
    "comments": [
        "uri: 4x latency improvement on login. Approve.",
        "security-vera: CRITICAL SECURITY REGRESSION: MD5 is cryptographically broken for password hashing. It has no salt, is vulnerable to rainbow tables, and can be brute-forced at billions of hashes per second on modern GPUs. This is a massive downgrade from bcrypt.",
        "walt: Strongly agree with vera. If this ships, a database breach would expose all passwords within hours.",
        "author-xena: But the latency improvement is significant...",
        "security-vera: Use bcrypt with reduced work factor (e.g., rounds=10 instead of 12) if latency is a concern. Never use MD5 for passwords."
    ],
    "files_changed": [
        "auth/password.py"
    ],
    "author": "xena",
    "base_branch": "main",
    "additions": 5,
    "deletions": 5,
    "gold": {
        "decision": "request_changes",
        "labels": ["security", "breaking-change"],
        "priority": "critical",
        "gold_keywords": ["password hashing", "MD5", "bcrypt", "downgrade"]
    }
}
