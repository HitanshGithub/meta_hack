{
    "pr_id": 2061,
    "title": "Remove rate limiting from public endpoint",
    "description": "Removes rate limiting from /api/public endpoints to allow unrestricted access for monitoring.",
    "diff": "diff --git a/middleware/rate_limit.py b/middleware/rate_limit.py\nindex 5a6b7c8..d9e0f1a 100644\n--- a/middleware/rate_limit.py\n+++ b/middleware/rate_limit.py\n@@ -8,7 +8,7 @@ RATE_LIMIT = 100\n \n-EXEMPT_PATHS = {\"/health\", \"/ready\"}\n+EXEMPT_PATHS = {\"/health\", \"/ready\", \"/api/public\"}\n \n class RateLimitMiddleware(BaseHTTPMiddleware):\n     async def dispatch(self, request, call_next):\n-        if request.url.path in EXEMPT_PATHS:\n+        if request.url.path in EXEMPT_PATHS or request.url.path.startswith(\"/api/public\"):\n             return await call_next(request)\n",
    "comments": [
        "beth: Simplifies monitoring. Approve.",
        "security-carl: Exempting /api/public/* from rate limiting enables DDoS and enumeration attacks.",
        "author-dana: Public endpoints are read-only.",
        "security-carl: Read-only endpoints still allow resource exhaustion."
    ],
    "files_changed": ["middleware/rate_limit.py"],
    "author": "dana",
    "base_branch": "main",
    "additions": 3,
    "deletions": 2,
    "gold": {
        "decision": "request_changes",
        "labels": ["security", "breaking-change"],
        "priority": "critical",
        "gold_keywords": ["rate limiting", "public endpoint", "DDoS", "abuse"]
    }
}
