{
    "pr_id": 2062,
    "title": "Improve API error messages with stack traces",
    "description": "Enhances error responses to include Python stack traces for easier debugging.",
    "diff": "diff --git a/api/error_handler.py b/api/error_handler.py\nindex 6b7c8d9..e0f1a2b 100644\n--- a/api/error_handler.py\n+++ b/api/error_handler.py\n@@ -1,5 +1,6 @@\n from __future__ import annotations\n \n+import traceback\n from fastapi import Request\n from fastapi.responses import JSONResponse\n \n@@ -8,6 +9,7 @@ async def handle_exception(request: Request, exc: Exception):\n     return JSONResponse(\n         status_code=500,\n         content={\n             \"error\": str(exc),\n+            \"traceback\": traceback.format_exc(),\n+            \"request_path\": str(request.url),\n         },\n     )\n",
    "comments": [
        "eric: Helpful for debugging. Approve.",
        "fay: Stack traces in API responses expose internal implementation details to attackers — file paths, library versions, variable names. This is an information disclosure vulnerability.",
        "author-greg: We only use this in dev.",
        "fay: This handler is registered globally. Guard it with a DEBUG check at minimum."
    ],
    "files_changed": [
        "api/error_handler.py"
    ],
    "author": "greg",
    "base_branch": "main",
    "additions": 3,
    "deletions": 0,
    "gold": {
        "decision": "request_changes",
        "labels": ["security"],
        "priority": "medium",
        "gold_keywords": ["error message", "stack trace", "information disclosure", "internals"]
    }
}
