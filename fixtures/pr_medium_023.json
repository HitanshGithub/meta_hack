{
    "pr_id": 2066,
    "title": "Add /healthz endpoint with dependency checks",
    "description": "Adds a comprehensive health check endpoint that verifies database and Redis connectivity.",
    "diff": "diff --git a/api/routes/health.py b/api/routes/health.py\nnew file mode 100644\nindex 0000000..c4d5e6f\n--- /dev/null\n+++ b/api/routes/health.py\n@@ -0,0 +1,22 @@\n+from __future__ import annotations\n+\n+from fastapi import APIRouter\n+from database import get_session\n+from cache import get_redis\n+\n+router = APIRouter()\n+\n+\n+@router.get(\"/healthz\")\n+async def health_check():\n+    checks = {}\n+    try:\n+        get_session().execute(\"SELECT 1\")\n+        checks[\"database\"] = \"ok\"\n+    except Exception:\n+        checks[\"database\"] = \"error\"\n+    try:\n+        get_redis().ping()\n+        checks[\"redis\"] = \"ok\"\n+    except Exception:\n+        checks[\"redis\"] = \"error\"\n+    status = \"healthy\" if all(v == \"ok\" for v in checks.values()) else \"degraded\"\n+    return {\"status\": status, \"checks\": checks}\n",
    "comments": [
        "quinn: Clean health check. Approve.",
        "rachel: LGTM, good dependency verification."
    ],
    "files_changed": [
        "api/routes/health.py"
    ],
    "author": "sam",
    "base_branch": "main",
    "additions": 22,
    "deletions": 0,
    "gold": {
        "decision": "approve",
        "labels": ["enhancement"],
        "priority": "low",
        "gold_keywords": ["health check", "endpoint", "monitoring", "dependency"]
    }
}
