{
    "pr_id": 2067,
    "title": "Improve validation error messages for API requests",
    "description": "Enhances Pydantic validation error messages to include field names and expected types in human-readable format.",
    "diff": "diff --git a/api/validation.py b/api/validation.py\nindex 8d9e0f1..a2b3c4d 100644\n--- a/api/validation.py\n+++ b/api/validation.py\n@@ -10,8 +10,12 @@ async def validation_exception_handler(request: Request, exc: RequestValidationE\n     errors = []\n     for error in exc.errors():\n         field = \" -> \".join(str(loc) for loc in error[\"loc\"])\n-        errors.append({\"field\": field, \"message\": error[\"msg\"]})\n-    return JSONResponse(status_code=422, content={\"errors\": errors})\n+        errors.append({\n+            \"field\": field,\n+            \"message\": error[\"msg\"],\n+            \"type\": error.get(\"type\", \"unknown\"),\n+        })\n+    return JSONResponse(status_code=422, content={\"detail\": errors, \"help\": \"Check field types and constraints.\"})\n",
    "comments": [
        "tara: Much better error messages now. Approve.",
        "uri: LGTM."
    ],
    "files_changed": [
        "api/validation.py"
    ],
    "author": "vera",
    "base_branch": "main",
    "additions": 6,
    "deletions": 2,
    "gold": {
        "decision": "approve",
        "labels": ["enhancement"],
        "priority": "low",
        "gold_keywords": ["validation", "error message", "user experience", "API"]
    }
}
