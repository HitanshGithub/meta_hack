{
    "pr_id": 2070,
    "title": "Add webhook notification for order status changes",
    "description": "Fires a webhook POST to a configurable URL whenever an order status transitions (created, paid, shipped, delivered).",
    "diff": "diff --git a/services/webhook_service.py b/services/webhook_service.py\nnew file mode 100644\nindex 0000000..e6f7a8b\n--- /dev/null\n+++ b/services/webhook_service.py\n@@ -0,0 +1,28 @@\n+from __future__ import annotations\n+\n+import json\n+import logging\n+import requests\n+from typing import Any\n+\n+logger = logging.getLogger(__name__)\n+\n+\n+class WebhookService:\n+    def __init__(self, webhook_url: str, secret: str) -> None:\n+        self._url = webhook_url\n+        self._secret = secret\n+\n+    def send(self, event: str, payload: dict[str, Any]) -> bool:\n+        body = {\"event\": event, \"data\": payload}\n+        headers = {\n+            \"Content-Type\": \"application/json\",\n+            \"X-Webhook-Secret\": self._secret,\n+        }\n+        try:\n+            resp = requests.post(self._url, json=body, headers=headers, timeout=10)\n+            resp.raise_for_status()\n+            return True\n+        except requests.RequestException as e:\n+            logger.error(\"Webhook delivery failed: %s\", e)\n+            return False\n",
    "comments": [
        "dana: Good feature. Tests needed for failure cases. Approve.",
        "eric: LGTM. Please add integration tests."
    ],
    "files_changed": [
        "services/webhook_service.py"
    ],
    "author": "fay",
    "base_branch": "main",
    "additions": 28,
    "deletions": 0,
    "gold": {
        "decision": "approve",
        "labels": ["enhancement", "needs-tests"],
        "priority": "medium",
        "gold_keywords": ["webhook", "notification", "event", "order"]
    }
}
