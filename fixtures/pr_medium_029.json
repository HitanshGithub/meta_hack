{
    "pr_id": 2072,
    "title": "Add CORS headers for new frontend domain",
    "description": "Updates CORS configuration to allow requests from the new app.example.com frontend domain.",
    "diff": "diff --git a/api/cors.py b/api/cors.py\nindex 9e0f1a2..b3c4d5e 100644\n--- a/api/cors.py\n+++ b/api/cors.py\n@@ -5,7 +5,7 @@ from fastapi.middleware.cors import CORSMiddleware\n def setup_cors(app: FastAPI) -> None:\n     app.add_middleware(\n         CORSMiddleware,\n-        allow_origins=[\"https://www.example.com\"],\n+        allow_origins=[\"*\"],\n         allow_credentials=True,\n         allow_methods=[\"*\"],\n         allow_headers=[\"*\"],\n",
    "comments": [
        "jane: Quick fix, approve.",
        "security-karl: BLOCKER: allow_origins=[\"*\"] with allow_credentials=True is a dangerous CORS misconfiguration. It allows any domain to make authenticated cross-origin requests to our API. An attacker site could steal session data. Use an explicit allowlist instead.",
        "author-lily: I'll narrow it to the two domains we need."
    ],
    "files_changed": [
        "api/cors.py"
    ],
    "author": "lily",
    "base_branch": "main",
    "additions": 1,
    "deletions": 1,
    "gold": {
        "decision": "request_changes",
        "labels": ["security", "bug"],
        "priority": "high",
        "gold_keywords": ["CORS", "wildcard", "origin", "credentials"]
    }
}
