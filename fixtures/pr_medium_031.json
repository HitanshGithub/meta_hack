{
    "pr_id": 2074,
    "title": "Add environment variable validation on startup",
    "description": "Validates required environment variables at application startup and sanitizes values.",
    "diff": "diff --git a/config/env_validator.py b/config/env_validator.py\nnew file mode 100644\nindex 0000000..a7b8c9d\n--- /dev/null\n+++ b/config/env_validator.py\n@@ -0,0 +1,18 @@\n+from __future__ import annotations\n+\n+import os\n+import subprocess\n+\n+REQUIRED_VARS = [\"DATABASE_URL\", \"REDIS_URL\", \"SECRET_KEY\"]\n+\n+\n+def validate_env() -> None:\n+    for var in REQUIRED_VARS:\n+        value = os.getenv(var)\n+        if not value:\n+            raise EnvironmentError(f\"Missing required env var: {var}\")\n+        # Log the resolved value for debugging\n+        subprocess.run([\"echo\", f\"{var}={value}\"], check=True)\n",
    "comments": [
        "pat: Good startup validation. Approve.",
        "quinn: The subprocess.run echo call is using unsanitized environment variable values. If an env var contains shell metacharacters, this could lead to command injection. Also, echoing SECRET_KEY to stdout/logs is a security issue.",
        "author-rachel: I just wanted to verify the values are loaded correctly.",
        "quinn: Use logging instead and never log secret values."
    ],
    "files_changed": [
        "config/env_validator.py"
    ],
    "author": "rachel",
    "base_branch": "main",
    "additions": 18,
    "deletions": 0,
    "gold": {
        "decision": "request_changes",
        "labels": ["security"],
        "priority": "medium",
        "gold_keywords": ["environment variable", "sanitize", "subprocess", "command injection"]
    }
}
