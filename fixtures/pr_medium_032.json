{
    "pr_id": 2075,
    "title": "Refactor: move models into separate files",
    "description": "Splits the monolithic models.py into individual model files. Introduces a circular import between User and Order models.",
    "diff": "diff --git a/models/user.py b/models/user.py\nnew file mode 100644\nindex 0000000..b8c9d0e\n--- /dev/null\n+++ b/models/user.py\n@@ -0,0 +1,10 @@\n+from __future__ import annotations\n+from models.order import Order\n+from database import Base\n+\n+class User(Base):\n+    __tablename__ = \"users\"\n+    id = Column(Integer, primary_key=True)\n+    name = Column(String(100))\n+    orders = relationship(\"Order\", back_populates=\"user\")\n+\ndiff --git a/models/order.py b/models/order.py\nnew file mode 100644\nindex 0000000..c9d0e1f\n--- /dev/null\n+++ b/models/order.py\n@@ -0,0 +1,9 @@\n+from __future__ import annotations\n+from models.user import User\n+from database import Base\n+\n+class Order(Base):\n+    __tablename__ = \"orders\"\n+    id = Column(Integer, primary_key=True)\n+    user_id = Column(Integer, ForeignKey(\"users.id\"))\n+    user = relationship(\"User\", back_populates=\"orders\")\n",
    "comments": [
        "sam: Circular import between user.py and order.py. Use string references.",
        "tara: Agree, this will fail with ImportError at runtime."
    ],
    "files_changed": ["models/user.py", "models/order.py"],
    "author": "uri",
    "base_branch": "main",
    "additions": 19,
    "deletions": 0,
    "gold": {
        "decision": "request_changes",
        "labels": ["bug"],
        "priority": "medium",
        "gold_keywords": ["circular import", "ImportError", "relationship", "dependency"]
    }
}
