{
    "pr_id": 2076,
    "title": "Add eager loading to user orders query",
    "description": "Loads user orders in the same query to reduce database roundtrips.",
    "diff": "diff --git a/repositories/user_repo.py b/repositories/user_repo.py\nindex 3d4e5f6..a7b8c9d 100644\n--- a/repositories/user_repo.py\n+++ b/repositories/user_repo.py\n@@ -10,8 +10,8 @@ class UserRepository:\n     def list_with_orders(self, limit: int = 50) -> list[User]:\n         '''List users with their orders.'''\n         session = get_session()\n-        users = session.query(User).limit(limit).all()\n-        # Each user.orders access triggers a separate query (N+1)\n+        users = session.query(User).limit(limit).all()\n+        # BUG: Still N+1 — orders are lazy-loaded per user access\n         for user in users:\n             _ = user.orders  # triggers lazy load\n         return users\n",
    "comments": [
        "vera: This still has the N+1 problem. The query fetches users, then each user.orders access fires another query. Use joinedload() or subqueryload().",
        "walt: Confirmed with query logging — 51 queries for 50 users. Please use selectinload(User.orders).",
        "author-xena: I thought accessing .orders in the same function would batch it."
    ],
    "files_changed": ["repositories/user_repo.py"],
    "author": "xena",
    "base_branch": "main",
    "additions": 2,
    "deletions": 2,
    "gold": {
        "decision": "request_changes",
        "labels": ["bug", "needs-tests"],
        "priority": "medium",
        "gold_keywords": ["N+1", "query", "eager loading", "performance"]
    }
}
